const KEY="brokenGalleryUnlockedV2";const PASSWORD="2142";const content=document.getElementById("galleryContent");document.getElementById("backBtn").onclick=()=>parent.postMessage({type:"PHONE_BACK"},"*");
function render(){if(localStorage.getItem(KEY)==="true"){showPhotos();}else{content.innerHTML=`<div class="locked-gallery"><div class="lock-panel"><div class="gallery-lock-icon">🔒</div><h2>Locked Gallery</h2><p>This album is protected.</p><input id="galleryPassword" class="gallery-input" inputmode="numeric" maxlength="4" placeholder="Password"><button id="unlockGallery" class="unlock-gallery">Unlock</button><p class="hint">Hint: the password is hidden somewhere in the Messages / Notes.</p><div id="galleryError" class="danger"></div></div></div>`;document.getElementById("unlockGallery").onclick=unlock;document.getElementById("galleryPassword").onkeydown=e=>{if(e.key==="Enter")unlock();}}}
function unlock(){const input=document.getElementById("galleryPassword");if(input.value===PASSWORD){localStorage.setItem(KEY,"true");showPhotos()}else document.getElementById("galleryError").textContent="Wrong password."}
function showPhotos(){let html='<div class="photos-grid">';for(let i=1;i<=18;i++)html+=`<div class="photo-tile"><img src="photo${i}.png" alt="Photo ${i}" onerror="this.style.display='none';this.nextElementSibling.style.display='grid'"><div class="placeholder">＋</div></div>`;html+='</div>';content.innerHTML=html}
render();
